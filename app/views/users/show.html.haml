.text_color
  .col-md-3
    %h1 portfolio quotes
    .jumbotron
      = render 'quotes/form'
      %table
        %thead
          %th
        %tbody
        - @quotes.each do |q|
          %tr
            %td= q.ticker

  .col-md-4
    %h1 profile
    .jumbotron
      = render 'form_profile'
